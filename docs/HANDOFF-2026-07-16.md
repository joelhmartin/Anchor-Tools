# Handoff — Anchor Tools, after 3.9.12 / 3.9.13

**Date:** 2026-07-16 · **`main` @ `6e8d8d1` = 3.9.13 (released)** · everything below is verified against the repo, not memory.

---

## 1. What just shipped

| Tag | Contents |
|---|---|
| **3.9.12** | Anchor **Events Manager rework** + a union-merge of the previously branch-tagged **3.9.11 social-feed YouTube-playlist** (so nothing regressed). |
| **3.9.13** | **Security patch** — Schema JSON-LD stored-XSS + schema AJAX authz hardening. |

**Events rework delivered:** event-type chooser (single / multisession / pick-one offerings / recurring); registration modes (WooCommerce / free / **external** link-or-embed + display-only price, fully client-agnostic); the `Occurrences` engine (parent→child reconcile, roster-safe soft-close, weekly/monthly recurrence) with `event_series` grouping + choose-your-date front end; per-type editable email templates + Monaco builder + read-only upcoming-sends panel; Event JSON-LD (every date via `subEvent`).

**Security patch (3.9.13):** `Anchor_Schema_Helper::minify_json()` / `validate_schema_json()` encoded JSON-LD with `JSON_UNESCAPED_SLASHES` and it was echoed raw into an inline `<script type="application/ld+json">` on `wp_head` → a stored value containing `</script>` broke out and executed (Contributor-reachable via `ajax_upload`, which had no per-post cap check). Fixed: dropped the flag at all encode sites, added an echo-site `</` → `<\/` guard, added per-post `current_user_can('edit_post', $post_id)` to **all four** schema AJAX write handlers, patched the dead `ACG_*` duplicate, added a regression test.

---

## 2. Architecture facts you must know before touching the events module

- **occurrence = event post.** Never subdivide a post. Pick-one/recurring materialize **child event posts** via `Occurrences::reconcile()` (idempotent; roster-safe **soft-close** — a child with seats is never deleted). Multisession = one post + a repeatable session list (display/schema only).
- **Engine-owned meta** (`group_id`, `group_role`, `offering_dates`, `occurrence_key`, `occurrence_closed`, `recurrence`) is `show_in_rest => false` and **deliberately excluded** from `save_meta()`'s `$input` allow-list — written only by dedicated validated paths. Don't "helpfully" add them.
- **The event CPT editor is Gutenberg.** Two hard-won consequences: (a) classic `redirect_post_location` + `admin_notices` notices **do not surface** — use inline metabox validation; (b) `show_in_rest => true` on classic-metabox-only meta causes a **last-write-wins save race** that silently reverts user input.
- **Zero changes were made** to shipped seat/tier/product-sync/order-reconcile internals. Keep it that way.
- **Backward compat:** old external events store their link in legacy `_anchor_event_registration_url`; `Module::external_url()` falls back to it and the one-time migration maps it forward. Legacy `registration_type`/`registration_url` meta is **read-only kept** (UI removed) — never blank it.
- **Docs:** `anchor-events-manager/EVENTS.md`, `EMAILS.md`, `EVENTS-WOOCOMMERCE.md`; spec + plan in `docs/superpowers/`.

---

## 3. Test harness (it exists — use it)

```bash
# PHPUnit (215 tests) — needs the WP test lib + a MySQL container on :3307
export WP_TESTS_DIR=/tmp/wordpress-tests-lib WP_CORE_DIR=/tmp/wordpress
composer install          # dev deps (dirties vendor/ — see §4)
composer test

# E2E (Playwright on @wordpress/env)
npm install && npm run wp-env start
npm run env:seed          # idempotent fixtures → e2e/.seed.json
npx playwright test --grep-invert "purchase"
```
If PHPUnit dies with a phpunit autoload error, you restored **prod** vendor — re-run `composer install`.
If the wp-env editor stops rendering metaboxes, **restart wp-env** (`stop && start`) — that fixes it.

---

## 4. LEFTOVERS

### A. Stranded unmerged branches — **highest value, same pattern that hid the email system**
Three branches hold real work, far behind `main`. The lifecycle-email system sat stranded on `feature/events-email-gap` this exact way until it was rescued into 3.9.12. **Audit these: is the work wanted?**

| Branch | Ahead | Behind |
|---|---|---|
| `feature/post-display-cpt` | +27 | −168 |
| `feature/facebook-video-embeds` | +11 | −102 |
| `feature/webinar-access-gating` | +4 | −168 |

**Prunable (fully merged into main):** `feat/site-config`, `feature/events-email-gap`, `feature/gallery-flex-mode`, `feature/gallery-full-customization`, `feature/gallery-plain-grid`, `feature/monaco-and-groups`, `feature/popup-scheduling`, `feature/post-display-cpt-restore`, `fix/gallery-conditional-deps`, `fix/gallery-flex-responsive`, `fix/gallery-popup-none-no-action` (+ their `origin/` counterparts).

### B. Repo hygiene
- **`vendor/` is committed prod-only, but `composer install` (dev) dirties `vendor/composer/autoload_*` + `installed.*`.** These are **not** gitignored → a `git add -A` would ship dev-autoload into the plugin. Always stage scoped paths. Consider `.gitignore`ing the churn or restoring prod vendor after testing.
- **Not gitignored (should be?):** `.phpunit.result.cache`, `package-lock.json` (decide: commit it or ignore it), local `vendor/{bin,phpunit,…}` dev dirs.
- ✅ `*.min.js|css` **are** correctly gitignored and generated by the release Action — no stale committed artifacts (verified: 0 tracked).
- **Delete the dead `ACG_*` files** — `includes/class-acg-admin.php`, `class-acg-render.php` (+ siblings) are a never-loaded duplicate of the Schema module (grep-confirmed: never `require`d/instantiated). They were patched defensively in 3.9.13 so the latent XSS can't be revived, but they should just be deleted.

### C. Events module — deferred (all reviewed as non-blocking)
1. **Series-archive ordering**: the group parent's own `start_ts` isn't synced to its earliest live child, so an archive mixing multiple groups can sort wrong. Fix = sync parent `start_ts`/`end_ts` in `Occurrences::reconcile()`. (JSON-LD already computes it correctly.)
2. **N+1 queries** rendering large recurring groups (bounded — recurrence capped at 104 occurrences).
3. **Untrash/restore of a group parent** not handled (children stay soft-closed/trashed).
4. **Dead front-end notice path**: `queue_group_notice()` hooks `redirect_post_location`, which the front-end manager form never applies, and `admin_notices` never fires on the front end → that branch is dead code. Remove or wire it properly.
5. **offering↔recurring type switch + guard failure** leaves the old type's children live until the next valid save (conservative, self-heals — arguably correct).
6. **`wp_kses` strips `<!DOCTYPE>`** from *customized* email templates (unmodified ones never touch kses). Cosmetic — mail clients strip it anyway.
7. **Duplicate timezone logic** — extract a shared `Module::resolve_timezone()`; `Event_Schema::resolve_timezone()` duplicates it and could drift.
8. `occurrence_label()` has a dead `label` -meta branch (the engine only bakes the label into the post title).
9. **Empty-URL external event** renders an empty external shell; the pre-rework "Registration link unavailable." notice is now unreachable for external mode.
10. An **old internal event with a stray `registration_url`** derives as `external` (unusual data state; pre-existing derive logic).
11. `handle_registration()` guards on legacy `registration_type`, not `registration_mode` (pre-existing; only reachable via a crafted POST since the internal form isn't rendered for external mode).
12. JSON-LD **`availability` is event-level, not per-tier** (documented cost/complexity tradeoff).
13. Cosmetic: empty "Tickets / Pricing" postbox header shows under non-`wc` modes; the `[event_registration]` auto-append can double-render a child's form (both renders identically gated — no double-booking risk).
14. Legacy `registration_type`/`registration_url` meta is kept read-only for BC. A future full migration could drop them.

### D. Testing
- **`e2e/purchase.spec.js` fails environmentally** (guest cart empty at checkout → no billing form). **Proven pre-existing**: fails identically on pristine `origin/main`, the Phase-0 tip, and HEAD — it is *not* a regression from any of this work. Needs a fresh wp-env or the guest-cart/session flow hardened.
- **Full-suite E2E flakes 1–2 tests** under single-worker wp-env load (~7 min); **every spec passes individually**. `event-grouping-authoring.spec.js` now self-resets its shared offering fixture per test.
- **Schema module has ~no test coverage** beyond the new XSS regression test — it's the module that just had a HIGH XSS.

### E. Process
- **Stop tagging releases off feature branches.** `main` sat at 3.9.08 while 3.9.09–3.9.11 were tagged off `feature/social-feed-youtube-playlist` — so `main` was missing shipped features and a naive release would have rolled them back. Reconciled by union-merging into 3.9.12. **Merge to `main` first, then tag from `main`.** (The release Action fires on any `N.N.N` tag push and builds the ZIP from that tag.)

---

## 5. Prompt for the next agent (full sweep of the EVENTS module)

> Paste this into a fresh session in this repo.

```
You are auditing the Anchor EVENTS MANAGER module of the Anchor Tools WordPress plugin
(/Volumes/G-DRIVE SSD/DEVELOPER/Anchor-Tools), currently main @ 3.9.13. SCOPE: anchor-events-manager/
(all classes, templates, assets), plus its tests/ and e2e/ specs and its docs. Do NOT audit other
modules. I want a comprehensive sweep for inconsistencies, latent bugs, security issues, and concrete
improvements WITHIN this module.

Read docs/HANDOFF-2026-07-16.md FIRST — it has the current state, the architecture invariants you must
not violate, the test harness, and a verified leftovers list (§4C is events-specific). Do not redo what
it says is already done, and do not re-litigate the invariants.

Use the `big-picture` skill — this is an exhaustive "find everything" audit of a large surface
(anchor-events-manager.php alone is ~200KB, class-woocommerce.php ~150KB, plus class-occurrences,
class-registrations, class-roster, class-ticket-types, class-product-sync, class-series,
class-event-schema, class-events-log, templates/, assets/). A tidy list of five findings would
undercount what's actually there. Fan out rather than reading linearly, and verify every claim against
the source before reporting it.

Essential context (verified — shapes where to look):
- This module was just heavily reworked and released in 3.9.12. The NEW layer (event types, the
  Occurrences engine, external registration, email templates + builder, Event JSON-LD) was built fast on
  top of an OLDER layer (WooCommerce integration, ticket tiers, seats/registrations, roster, product
  sync). The seams BETWEEN those layers are the most likely place for inconsistency — look hard there.
- INVARIANTS — do not propose "fixing" these, they are deliberate:
  * occurrence = event post (never subdivide a post); pick-one/recurring materialize child event posts
    via Occurrences::reconcile(), which is idempotent and roster-safe (a child with seats is soft-closed,
    never deleted).
  * Engine-owned meta (group_id, group_role, offering_dates, occurrence_key, occurrence_closed,
    recurrence, email templates) is show_in_rest=false and deliberately OUT of save_meta()'s allow-list.
  * The event CPT editor is Gutenberg: classic redirect_post_location/admin_notices do NOT surface (use
    inline metabox validation), and show_in_rest=true on classic-only meta causes a save race.
  * Legacy _anchor_event_registration_type/_registration_url meta is kept read-only for backward compat
    (Module::external_url() falls back to it; a migration maps it forward). Never blank it.
- A HIGH stored XSS was just found+fixed in a SIBLING module (Schema): JSON_UNESCAPED_SLASHES + raw echo
  into an inline <script>, reachable by a Contributor via an AJAX handler with NO per-post capability
  check. Hunt that same CLASS here: every AJAX handler in this module (email preview, storefront
  add-to-cart, registration submit, roster actions) for missing nonce/capability checks — especially
  missing per-object checks like current_user_can('edit_post', $id) — plus any echo of user data without
  escaping, and any $_POST-derived meta/option written without sanitization. Note the module already
  does escape email tokens and kses the external embed + email templates; verify those hold rather than
  assuming.

What I want out of it:
1. SECURITY — ranked, each with file:line, the exact reachable path (which capability/role), a concrete
   payload/repro, and the minimal fix. Distinguish CONFIRMED-exploitable from theoretical. Explicitly say
   when something is NOT exploitable and why — do not pad the list.
2. CORRECTNESS — latent bugs and data-integrity risks, weighted toward: the Occurrences reconcile edge
   cases; capacity/seat accounting and the per-event MySQL lock; the WooCommerce order->seat reconcile
   and product/variation sync; the reminder/roster cron and its idempotency markers; timezone/DST
   handling; anything that could orphan or double-count a roster.
3. CONSISTENCY — where the new layer and the old layer disagree: duplicated logic that should be shared,
   two ways of resolving the same thing, drift from the house conventions in CLAUDE.md (asset loading,
   update_option autoload=false, text domain 'anchor-schema', AJAX prefix anchor_events_, admin-asset
   gating, jQuery IIFE), dead/never-reached code, half-wired features.
4. STRUCTURE — anchor-events-manager.php is ~200KB and does far too much. Propose a concrete, low-risk
   split (what to extract, in what order, what would break), or argue against it if the risk outweighs it.
5. TESTS — the module has 215 PHPUnit tests + Playwright E2E (harness in handoff §3; reuse it). Assess
   QUALITY not just count: find vacuous/tautological tests, and name the highest-risk UNTESTED paths
   (I suspect the WC order-reconcile, product sync, and roster paths). Recommend the cheapest
   high-value tests to add.
6. Triage the events leftovers in handoff §4C, and go beyond them.

Rules: read-only audit — do NOT change code, do NOT commit, do NOT push. Never use browser/Chrome
automation. Report a prioritized document with file:line evidence and a clear "what I'd do first".
Flag anything you could not verify rather than asserting it.
```
