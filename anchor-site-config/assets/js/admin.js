/* Anchor Site Config — admin scripts */
( function( $ ) {
    'use strict';

    $( function() {
        // Initialize WP Iris color pickers.
        $( '.anchor-color-field' ).wpColorPicker();
    });
} )( jQuery );
